# NXT Step Navigator — Main Page Starter

This package contains the rebuilt NXT Step Navigator homepage only.

Included now:
- `index.html`
- `css/style.css`
- `js/main.js`
- logo and banner assets in `assets/`
- favicon and Apple touch icon
- empty document folders with `.gitkeep` files so GitHub keeps them

Not included yet:
- Counselor Compass section pages
- WHS Counselor Compass section pages
- NextStep How-To section pages
- NextStep Tours section pages
- Resource Library section pages

The homepage cards are marked as “Page coming next” and are temporarily handled by JavaScript so visitors do not land on unfinished pages.
